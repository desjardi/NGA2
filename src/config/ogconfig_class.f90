!> One-grid config concept is defined here.
!> The intention is to have a concept that mimics the original NGA,
!> which will be evolved into mg_config (multi grid), and ag_config (adaptive grid)
module ogconfig_class

  implicit none
  
  
  
end module ogconfig_class
